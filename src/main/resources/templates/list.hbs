
{{cyan divider}} {{bold title}}
  {{#empty archives ~}}
  No local archives found
  {{else ~}}
  {{#archives ~}}  
  {{yellow name}} ({{underline version}}{{versions}})
  {{/archives ~}}
  {{/empty ~}}
{{{footer}}}